// Minha estrutura para o formulário de contato.

export interface Contato {
    name: string;
    email: string;
    phone: string;
    service: string;
    location: string;
    message: string;
}
